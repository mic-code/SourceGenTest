# IIncrementalGenerator sample

## Some observation:

source gen class library must target `netstandard2.0` for visual studio to work correctly
dotnet run works even when not targeting `netstandard2.0`


